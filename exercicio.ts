function multiplicacao(a: number, b: number, c: number): number {
  return a * b * c;
}

function saudar(nome: string): string {
  return `Olá ${nome}`;
}

console.log(multiplicacao(3, 5, 18));
console.log(saudar("Renan"));